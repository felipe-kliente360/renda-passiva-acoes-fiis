# Runtime do pipeline. Versões mínimas; o CI fixa via pip.
pandas>=2.2
pyarrow>=15
pyyaml>=6
requests>=2.31
# Fallback de preços (opcional em runtime; brapi é o primário e usa só requests)
yfinance>=0.2.40
# Dev
pytest>=8
ruff>=0.5
